describe('MarginAppV1', () => {
  it('succeed when all conditions are obeyed');

  it('revert when more than 2 participants');

  it('revert when more than 1 asset');

  it('revert when more than 2 allocations');

  it('revert when allocation sum changed');
});
